{
  "name": "feedbin-to-pinboard",
  "version": "1.0.0",
  "main": "handler.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kepford/serverless-feedbin-to-pinboard.git"
  },
  "scripts": {
    "sls": "serverless",
    "offline": "serverless offline start",
    "deploy-dev": "serverless deploy -s dev -v",
    "deploy-test": "serverless deploy -s test -v",
    "deploy-prod": "serverless deploy -s prod -v",
    "log-dev": "serverless logs -t -s dev  -v -f ",
    "log-test": "serverless logs -t -s test -v -f ",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.648.0",
    "node-fetch": "^2.6.0",
    "node-pinboard": "^1.0.0"
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "eslint-config-prettier": "^6.10.1",
    "eslint-plugin-prettier": "^3.1.2",
    "prettier": "2.0.4",
    "serverless": "^3.22.0",
    "serverless-offline": "^10.3.2"
  },
  "description": "Takes a feed of Feedbin entries and creates Pinboard bookmarks."
}
